# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 환경 변수 — .env 만 제외 (.env.example 등은 저장소에 포함)
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# 로컬 전용(인증키·비밀) — 커밋·PR에 포함되면 안 됨. CI는 GitHub Actions Secrets만 사용.
.env*.local
.env.local

# 로컬 임시·대용량 덤프 (포털 JSON 복사 등)
tmp/
